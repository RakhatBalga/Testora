This directory is reserved for Feature-Sliced Design entities.

Move domain-owned models, typed API hooks, stores, and entity UI here during
incremental refactors. Do not put page composition or cross-entity workflows in
this layer.
